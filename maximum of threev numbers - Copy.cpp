#include<iostream>
using namespace std;
int main()
{
	int i,n;cout<<"enter the number  = "<< i+1;
	 	cin>> arr[i];
	 }
	for(i=0;i<n;++i)
	 {
	 	if(arr[0]<arr[i])
	 	 arr[0] = arr[i];
	 }
	cout<<endl<<"largest element = "<< arr[i<100];
	return 0;
}
	float arr[100];
	 cout<<"enter the total number of elements" ;
      cin>>n;
	   cout<<endl;
	for(i=0;i<n;++i)
}
	 	
